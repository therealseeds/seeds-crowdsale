function enableOtherOption(elem) {
  document.getElementById('option-other-input').style.visibility = elem.checked ? "visible" : "hidden";
}

function enableOtherCategory(elem) {
  document.getElementById('category-other-input').style.visibility = elem.checked ? "visible" : "hidden";
}
