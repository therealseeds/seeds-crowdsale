test:
  post:
    - rm -rf node_modules
deployment:
  production:
    branch: master
    codedeploy:
      crowdsale:
        deployment_group: crowdsale-dg
general:
  branches:
    only:
      - master
